server:
  port: 8888

spring:
  application:
    name: medicare-config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/KienPhan1109/medicare-config-repo
          default-label: main
          clone-on-start: true
