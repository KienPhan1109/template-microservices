server:
  port: 8084

spring:
  application:
    name: medicare-pharmacy-service

  datasource:
    url: jdbc:mysql://localhost:3306/medicare_pharmacy_db?createDatabaseIfNotExist=true&useSSL=true&allowPublicKeyRetrieval=true
    username: root
    password: Kien@1109AA.
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

  jackson:
    date-format: dd/MM/yyyy HH:mm:ss
    time-zone: Asia/Ho_Chi_Minh
